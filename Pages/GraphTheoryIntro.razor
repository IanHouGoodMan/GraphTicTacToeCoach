@page "/graph-theory-intro"

<PageTitle>图论入门</PageTitle>

<section class="hero">
    <p class="eyebrow">第一课</p>
    <h1>什么是「图」？🌐</h1>
    <p class="lead">
        在数学里，<strong>图</strong>不是一幅画，而是<strong>「点」</strong>和<strong>「线」</strong>组成的关系网。
        点叫<em>顶点</em>，线叫<em>边</em>。
    </p>
</section>

<section class="panel">
    <h2>例 1：好朋友图 👧🧒</h2>
    <p>每个小朋友是一个点，两个人是好朋友就连一条线。</p>

    <div style="overflow-x:auto;">
    <svg viewBox="0 0 420 240" width="100%" style="max-width:520px; display:block; margin:0 auto;" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="好朋友图">
        <!-- edges -->
        <line x1="80"  y1="60"  x2="220" y2="40"  stroke="#feb47b" stroke-width="4" />
        <line x1="80"  y1="60"  x2="160" y2="160" stroke="#feb47b" stroke-width="4" />
        <line x1="220" y1="40"  x2="340" y2="80"  stroke="#feb47b" stroke-width="4" />
        <line x1="220" y1="40"  x2="280" y2="180" stroke="#feb47b" stroke-width="4" />
        <line x1="160" y1="160" x2="280" y2="180" stroke="#feb47b" stroke-width="4" />
        <line x1="340" y1="80"  x2="280" y2="180" stroke="#feb47b" stroke-width="4" />

        <!-- nodes -->
        @{
            (string n, int x, int y, string c)[] friends =
            {
                ("小明", 80, 60, "#ffd86f"),
                ("小红", 220, 40, "#ff8fa3"),
                ("小亮", 340, 80, "#74c0fc"),
                ("小芳", 160, 160, "#b2f2bb"),
                ("小华", 280, 180, "#ffc078"),
            };
        }
        @foreach (var f in friends)
        {
            <circle cx="@f.x" cy="@f.y" r="26" fill="@f.c" stroke="#3a2c1f" stroke-width="2" />
            @((MarkupString)$"<text x='{f.x}' y='{f.y + 5}' text-anchor='middle' font-size='14' font-weight='700' fill='#2f2620'>{f.n}</text>")
        }
    </svg>
    </div>

    <p class="small-note" style="text-align:center;">
        ⬆️ 5 个顶点，6 条边。每条边表示「这两个人是好朋友」。
    </p>
</section>

<section class="panel">
    <h2>例 2：井字棋也是一张图 🎯</h2>
    <p>把空棋盘当成第一个点。X 走出第一步后，棋盘就跳到一个「新点」上。</p>

    <div class="tree">
        <div class="tree-root">
            <MiniBoard Board="@Empty9" Size="100" Aria="空棋盘" />
            <div class="node-cap">空棋盘（开始）</div>
        </div>
        <svg class="tree-edges" viewBox="0 0 600 60" preserveAspectRatio="none" aria-hidden="true">
            <line x1="300" y1="0" x2="100" y2="60" stroke="#5cb3ff" stroke-width="3" />
            <line x1="300" y1="0" x2="300" y2="60" stroke="#5cb3ff" stroke-width="3" />
            <line x1="300" y1="0" x2="500" y2="60" stroke="#5cb3ff" stroke-width="3" />
        </svg>
        <div class="tree-children">
            <div class="tree-child">
                <MiniBoard Board="@(new[] {'X',' ',' ',' ',' ',' ',' ',' ',' '})" Size="100" />
                <div class="node-cap">走「角」</div>
            </div>
            <div class="tree-child">
                <MiniBoard Board="@(new[] {' ','X',' ',' ',' ',' ',' ',' ',' '})" Size="100" />
                <div class="node-cap">走「边」</div>
            </div>
            <div class="tree-child">
                <MiniBoard Board="@(new[] {' ',' ',' ',' ','X',' ',' ',' ',' '})" Size="100" />
                <div class="node-cap">走「中心」</div>
            </div>
        </div>
    </div>

    <p class="small-note" style="text-align:center; margin-top:0.6rem;">
        ⬆️ 同样位置（旋转、镜像）算同一种走法，所以 X 第一步只有 3 种本质不同的选择。
    </p>
</section>

<section class="panel lesson">
    <h2>新词卡 ✨</h2>
    <ol>
        <li><strong>顶点 / 节点</strong>：图里的小圆点，可以代表「一个状态」「一个人」「一座城市」。</li>
        <li><strong>边</strong>：连接两个顶点的线，表示一种「关系」或「一步操作」。</li>
        <li><strong>路径</strong>：从一个顶点沿着边走到另一个顶点经过的路。</li>
        <li><strong>树</strong>：一种特别的图——从一个根出发，永远不会绕回来。</li>
    </ol>
    <p>
        👉 <a href="play">先去下两盘井字棋找找感觉</a>，
        然后去 <a href="analysis">图论分析</a> 看看每一步背后的图。
    </p>
</section>

@code {
    private static readonly char[] Empty9 = new[] { ' ', ' ', ' ', ' ', ' ', ' ', ' ', ' ', ' ' };
}
