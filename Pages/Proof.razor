@page "/proof"
@inject TicTacToeEngine Engine

<PageTitle>和局证明</PageTitle>

<section class="hero">
    <p class="eyebrow">小小数学家</p>
    <h1>为什么井字棋一定平局？📜</h1>
    <p class="lead">
        如果两个人都很聪明，井字棋的最后结果一定是<strong>平局</strong>。
        我们用「图」来说明这个事情。
    </p>
</section>

<section class="panel">
    <h2>第 1 步：把所有棋盘连成一张大图</h2>
    <p>
        每个棋盘是一个<strong>点</strong>，每走一步是一条<strong>边</strong>。
        从空棋盘开始，所有可能的走法连起来就是一棵<strong>博弈树</strong>。
        这棵树最多有 9 层（因为最多走 9 步）。
    </p>
    <ul>
        <li>X 在<em>奇数层</em>选边（轮到 X 走），它要走向「最大值」的子节点。</li>
        <li>O 在<em>偶数层</em>选边（轮到 O 走），它要走向「最小值」的子节点。</li>
    </ul>
</section>

<section class="panel">
    <h2>第 2 步：从下往上「打分」</h2>
    <p>
        我们给每个终局一个分数：
        <span class="status-pill x">X 赢 = +1</span>
        <span class="status-pill draw">平 = 0</span>
        <span class="status-pill o">O 赢 = −1</span>。
    </p>
    <p>然后从树的最底层往上算：</p>

    <div class="tree">
        <div class="tree-root" style="background:#fff9db; border-color:#ffd43b;">
            <strong>当前节点的分数</strong>
            <div class="node-cap">轮到 X：取最大；轮到 O：取最小</div>
        </div>
        <svg class="tree-edges" viewBox="0 0 600 60" preserveAspectRatio="none" aria-hidden="true">
            <line x1="300" y1="0" x2="120" y2="60" stroke="#74c0fc" stroke-width="3" />
            <line x1="300" y1="0" x2="300" y2="60" stroke="#74c0fc" stroke-width="3" />
            <line x1="300" y1="0" x2="480" y2="60" stroke="#74c0fc" stroke-width="3" />
        </svg>
        <div class="tree-children">
            <div class="tree-child" style="background:#ebfbee; border-color:#74c476;">+1（X 赢）</div>
            <div class="tree-child" style="background:#fff9db; border-color:#ffd43b;">0（平）</div>
            <div class="tree-child" style="background:#fff5f5; border-color:#ff8787;">−1（O 赢）</div>
        </div>
    </div>
</section>

<section class="panel">
    <h2>第 3 步：根节点的分数 = 整盘棋的结果</h2>
    <p>
        我用电脑现在就算给你看：从空棋盘出发，走最优棋的最终分数是
        <span class="status-pill draw" style="font-size:1.05rem;">@RootScoreText（@RootScore）</span>。
    </p>
    <p>
        所以——不管 X 怎么走，O 总能找到一条边，让结果不会比「平局」更差；
        反过来 X 也一样。两个人都不会输，就是<strong>和局</strong>。 ✨
    </p>
</section>

<section class="panel">
    <h2>第 4 步：为什么会这样？（直觉）</h2>
    <ol>
        <li><strong>中心是关键。</strong>占了中心的格子，能参与 4 条赢线（横、竖、两条对角线）。</li>
        <li><strong>双威胁。</strong>要赢就要造出「我同时威胁两条线」，对手只能堵一条。</li>
        <li><strong>对手也会用同样办法。</strong>所以只要一方不犯错，另一方就找不到双威胁。</li>
        <li><strong>9 个格子有限。</strong>有限多个棋盘 = 有限多个图节点，电脑可以全部算完，结论：根 = 0。</li>
    </ol>
</section>

<section class="panel">
    <h2>🔬 试一试</h2>
    <p>
        想验证「和局」？去 <a href="vs-ai">人机对战</a> 把难度调到「完美」，
        无论你怎么下，最多只能逼平。
        想看每个节点的分数？去 <a href="analysis">图论分析</a> 一步步看。
    </p>
</section>

@code {
    private int RootScore => Engine.Minimax(new char[9] {' ',' ',' ',' ',' ',' ',' ',' ',' '}, TicTacToeEngine.X);
    private string RootScoreText => TicTacToeEngine.ScoreLabel(RootScore);
}
