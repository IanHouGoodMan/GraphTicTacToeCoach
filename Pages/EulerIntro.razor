@page "/euler"
@inject EulerService Euler

<PageTitle>一笔画入门</PageTitle>

<section class="hero">
    <p class="eyebrow">图论第二课 · 欧拉路径</p>
    <h1>什么是「一笔画」？🖊️</h1>
    <p class="lead">
        给你一张图，让你拿起笔，<strong>笔不离纸</strong>，每条边只走一次，把整张图画完——这就叫
        <em>一笔画</em>。
    </p>
</section>

<section class="panel">
    <h2>故事：柯尼斯堡的七座桥 🌉</h2>
    <p>
        300 年前，有座城叫柯尼斯堡，城里有 7 座桥。
        城里人想了好久：能不能<strong>每座桥只走一次</strong>，把 7 座桥全走完？
    </p>
    <p>有个数学家叫<strong>欧拉</strong>，他说：「我们把岸和岛画成<em>点</em>，把桥画成<em>线</em>，看看图就知道了！」</p>

    <div class="graph-stage">
        <GraphCanvas Puzzle="@Konigsberg" ShowDegrees="true" Aria="柯尼斯堡七桥" />
        <p class="small-note" style="text-align:center;">
            红色 = 奇度顶点（连了奇数条线）。这张图有 <strong>4 个奇点</strong>。
        </p>
    </div>
</section>

<section class="panel">
    <h2>欧拉的发现 ✨</h2>
    <p>欧拉爷爷想了想，发现了一条<strong>规律</strong>：</p>

    <div class="rule-card">
        🌟 一张<strong>连通的图</strong>能一笔画 ⇔ <br/>
        <strong>奇度顶点</strong>的个数是 <span class="status-pill draw">0</span> 或 <span class="status-pill draw">2</span>。
    </div>

    <ul>
        <li>奇点 = <strong>0 个</strong> → 任意点出发，最后能<strong>回到起点</strong>（叫「欧拉回路」）</li>
        <li>奇点 = <strong>2 个</strong> → 必须从其中<strong>一个奇点</strong>出发，走到<strong>另一个奇点</strong>结束</li>
        <li>奇点 ≥ 4 个 → <strong>不可能</strong>一笔画</li>
    </ul>

    <p>所以柯尼斯堡七桥的答案是：<strong>走不通</strong>！因为有 4 个奇点。😢</p>
</section>

<section class="panel">
    <h2>为什么呢？想一想 🤔</h2>
    <p>除了起点和终点，每<strong>经过</strong>一个点，都要「走进去 + 走出来」，是 <strong>2 条边</strong>一对。</p>
    <p>所以中间这些点，连的边数一定是<strong>偶数</strong>。</p>
    <p>奇点只能当<strong>起点或终点</strong>——一张图最多只能有 <strong>2 个</strong>奇点！</p>
</section>

<section class="panel">
    <h2>来看几个例子 👀</h2>
    <div class="example-grid">
        @foreach (var p in Examples)
        {
            var v = Euler.Analyse(p);
            <div class="example-card">
                <div class="example-title">
                    @p.Name
                    @if (v.Drawable)
                    { <span class="status-pill draw">能 ✅</span> }
                    else
                    { <span class="status-pill o">不能 ❌</span> }
                </div>
                <GraphCanvas Puzzle="@p" ShowDegrees="true" OddVertices="@v.OddVertices" Aria="@p.Name" />
                <p class="small-note">奇度顶点：<strong>@v.OddCount</strong> 个</p>
            </div>
        }
    </div>
</section>

<section class="panel lesson">
    <h2>下一步 👇</h2>
    <ol>
        <li>去 <a href="euler/practice">🎮 一笔画练习</a> 抽一道题，先猜「能 / 不能」，再动手画！</li>
        <li>不会的题去 <a href="euler/analysis">🔬 一笔画分析</a> 看电脑帮你算奇点和笔顺。</li>
    </ol>
</section>

@code {
    private GraphPuzzle Konigsberg = default!;
    private GraphPuzzle[] Examples = default!;

    protected override void OnInitialized()
    {
        Konigsberg = Euler.GetById("konigsberg");
        Examples = new[]
        {
            Euler.GetById("triangle"),
            Euler.GetById("house"),
            Euler.GetById("envelope"),
            Euler.GetById("k4"),
        };
    }
}
